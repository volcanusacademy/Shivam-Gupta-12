//C Program to add numbers

#include <stdio.h>

int main()
{
    int a,b,add;
    printf("enter the number:");
    scanf("%d",&a);
    printf("enter the number:");
    scanf("%d",&b);
    
    {
        add=a+b;
    }
    
    printf("addition of two number=%d",add);

    return 0;
}