/*Program to get marks of 5 subjects from user and find out its grade using nested if else

Per>=75 && per<=100 Grade A

Per>=60 && per<75 Grade B

Per>=50 && per<60 Grade C

Per>=33 && per<50 Grade D

else Fail

Do Same with else if ladder.

*/

#include <stdio.h>

int main()
{
    int Hindi,English,Science, Maths,Sanskrit; 
    float per; 


    printf("Enter Hindi marks: \n");
    scanf("%d", &Hindi);
    
    printf("Enter English marks: \n");
    scanf("%d", &English);
    
    printf("Enter Science marks: \n");
    scanf("%d", &Science);
    
    printf("Enter Maths marks: \n");
    scanf("%d", &Maths);
    
    printf("Enter Sanskrit marks: \n");
    scanf("%d", &Sanskrit);
    

    per = (Hindi+English+Science+Maths+Sanskrit) / 5.0;

    printf("Percentage = %f\n", per);


    if(per >=75)
    {
        printf("Grade A");
    }
    else if(per >= 65)
    {
        printf("Grade B");
    }
    else if(per >=50 )
    {
        printf("Grade C");
    }
    else if(per >= 33)
    {
        printf("Grade D");
    }
   
    else
    {
        printf("Grade F");
    }

    return 0;
}